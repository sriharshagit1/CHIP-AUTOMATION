# ChipPilot V1 demo

Run the deterministic end-to-end demo locally:

```bash
python -m scripts.demo_v1
```

It investigates four benchmark cases and writes per-case JSON reports plus `evidence/demo/summary.json`.

The demo uses controlled benchmark handlers. It is intended to validate the execution/reporting pipeline, not to represent LLM benchmark performance.
